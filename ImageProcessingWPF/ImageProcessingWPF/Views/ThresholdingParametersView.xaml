<UserControl x:Class="ImageProcessingWPF.Views.ThresholdingParametersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ImageProcessingWPF.Views"
             mc:Ignorable="d">
    <StackPanel Orientation="Horizontal">
        <TextBlock Text="max deviation: "/>
        <TextBox Width="40" Height="30" FontSize="18" Margin="0,2.5,5,0"
                 Text="{Binding MaxDeviation}"/>
        <TextBlock Text="mean area size: "/>
        <TextBox Width="40" Height="30" FontSize="18" Margin="0,2.5,5,0"
                 Text="{Binding MeanAreaSize}"/>
    </StackPanel>
</UserControl>
